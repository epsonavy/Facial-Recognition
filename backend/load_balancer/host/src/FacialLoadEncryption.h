//they just return buffer as is right now, this will be available for both host and peer
const char* salt = "PETERISGUY";

//Loads an encrypted file into a buffer
char* encrypt_load(char* buffer){
	return buffer;
}

//Decrypts loaded file from buffer
char* decrypt_load(char* buffer){
	return buffer;
}


